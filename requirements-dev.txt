# Slim dependency set for CI / unit tests. The full requirements.txt pulls
# chromadb + lancedb + sentence-transformers (heavy ML wheels) that the unit
# suite does not exercise — these are everything the tests import.
httpx>=0.27.0
pydantic>=2.0.0
jsonschema>=4.0.0
redis>=5.0.0
fastapi>=0.110.0
anthropic>=0.40.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
flake8>=7.0.0
