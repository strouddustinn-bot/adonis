[pytest]
asyncio_mode = auto
testpaths = tests
# Put the repo root on sys.path so `pytest` (not just `python -m pytest`)
# can import the top-level packages (routing, tools, openclaw, …) in CI.
pythonpath = .
python_files = test_*.py
filterwarnings =
    ignore::DeprecationWarning
